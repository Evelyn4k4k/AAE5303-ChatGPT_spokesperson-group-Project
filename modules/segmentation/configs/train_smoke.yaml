# Short run to verify install + scripts (not for final leaderboard quality).
epochs: 3
batch_size: 2
learning_rate: 0.0001
validation_percent: 10.0
scale: 0.5
amp: true
bilinear: false
classes: 2
load_checkpoint: null
