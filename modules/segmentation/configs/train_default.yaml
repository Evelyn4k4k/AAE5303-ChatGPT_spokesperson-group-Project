# Default hyperparameters for Pytorch-UNet-style training on AMtown02 (or any paired imgs/masks folder).
# Paths are resolved relative to modules/segmentation/ by scripts/train_unet.py

epochs: 40
batch_size: 4
learning_rate: 0.0001
validation_percent: 10.0
scale: 0.5
amp: true
bilinear: false
classes: 2
load_checkpoint: null
